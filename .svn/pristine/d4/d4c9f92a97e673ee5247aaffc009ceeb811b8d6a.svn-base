package com.sl.tune.repository;

import java.util.List;

import com.sl.tune.entity.Channel;

public interface ChannelRepository {
		
	void insert(Channel clipEntity);
	
	Channel removeOne(String id);

	Channel find(String id);
	
	List<Channel> findAll();
		
}
